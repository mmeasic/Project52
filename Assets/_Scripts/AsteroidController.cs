using UnityEngine;
using System.Collections;

//Just a simple script to make the asteroids of the "Main" scene rotate. 
public class AsteroidController : MonoBehaviour {
	
/*	void Start()
	{
		if((GameObject.FindGameObjectWithTag ("Asteroids").transform.position.x<-740)||
			(GameObject.FindGameObjectWithTag ("Asteroids").transform.position.x>740)||
			(GameObject.FindGameObjectWithTag ("Asteroids").transform.position.z<-740)||
			(GameObject.FindGameObjectWithTag ("Asteroids").transform.position.x>740)){
		Destroy (gameObject);
	}*/


	//}
}